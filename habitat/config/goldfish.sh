#!/bin/bash

export VAULT_ADDR="{{cfg.addr}}"
export VAULT_TOKEN="{{cfg.token}}"
export DEV_MODE="{{cfg.dev_mode}}"
export GOLDFISH_ADDR="{{cfg.goldfish_addr}}"
export CERT_FILE="{{cfg.cert_file}}"
export KEY_FILE="{{cfg.key_file}}"
